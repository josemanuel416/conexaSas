-- ============================================================
-- Seed: Usuario administrador del sistema
-- La contraseña se inserta desde el script de migración Node.js
-- usando la variable ADMIN_PASSWORD del .env
-- ============================================================

-- Este archivo es referencia. El admin se crea en migrate.js
-- con el hash bcrypt generado en tiempo de ejecución.
